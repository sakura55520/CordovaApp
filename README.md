> 海纳前端pad项目

> base api配置在`src/api/global/apiBase.js`

## Build Setup

``` bash
# install dependencies
npm install

# serve with hot reload
npm run dev

# 构建公司搭建的云测试环境
npm run hcp:yuntest

# build for production
npm run hcp
```

